# 🧩 คู่มือการใช้งานไฟล์ `config.h`

ไฟล์ `config.h` ใช้สำหรับ **ตั้งค่าหลักของโปรแกรม** โดยไม่ต้องแก้โค้ดในส่วนระบบหลัก (`main_base.cpp`, `CroLibMotion.cpp` ฯลฯ)  
เหมาะสำหรับลูกค้าหรือผู้ใช้งานที่ต้องการปรับพฤติกรรมของระบบ เช่น **แพทเทิร์นการเคลื่อนไหว**, **ความเร็ว**, **ระดับแสง**, และ **ช่วงเวลา** ของการทำงาน

---

## 🔧 1. จุดประสงค์ของ `config.h`

- แยกค่าที่ “ผู้ใช้สามารถแก้ไขได้” ออกจากโค้ดหลัก  
- ลดความเสี่ยงจากการแก้ไข logic ภายในระบบ  
- ให้ลูกค้าสามารถปรับ pattern ใหม่ หรือทดสอบค่าต่าง ๆ ได้ง่าย  
- สามารถเลือก pattern ที่ต้องการทำงานได้จากตัวแปรเดียว (`sequence_selection`)

---

## ⚙️ 2. รายละเอียดการตั้งค่า

### 🔹 2.1 การเลือกแพทเทิร์นที่ใช้งานเริ่มต้น

```cpp
uint8_t sequence_selection = 1; // 1 = Pattern 1, 2 = Pattern 2
```
> ใช้เลือกว่าจะให้เริ่มทำงานด้วยแพทเทิร์นใด  
> โปรแกรมจะสามารถสลับอัตโนมัติตามค่าความสว่างแสง (lux) ได้ด้วย

---

### 🔹 2.2 การตั้งค่าพรีเซ็ตของมอเตอร์

```cpp
struct MotorPreset
{
    uint8_t speed;   // ความเร็ว (0–255)
    uint16_t degree; // มุมหมุน (องศา)
    uint8_t times;   // จำนวนครั้งในการหมุนต่อรอบ
};

static MotorPreset M1_PRESET{100, 40, 1};
static MotorPreset M2_PRESET{100, 360, 1};
```

> สามารถแก้ไขค่าตามความต้องการได้  
> เช่น เพิ่มความเร็ว หรือเปลี่ยนองศาการหมุนของแต่ละมอเตอร์

---

### 🔹 2.3 การตั้งค่าช่วงแสง (Lux Threshold)

```cpp
#define LIGHT_SENSOR_THRESHOLD_LUX_MIN  50.0f
#define LIGHT_SENSOR_THRESHOLD_LUX_MID  300.0f
#define LIGHT_SENSOR_THRESHOLD_LUX_MAX  1000.0f
```

---

### 🔹 2.4 การตั้งค่าระยะเวลาและจำนวนลำดับ (Sequence)

```cpp
#define PATTERN_NUM_SEQUENCES 19      // จำนวน step ต่อ pattern
#define PATTERN_TIMEFRAME_MS  3000    // เวลาต่อ step (หน่วย ms)
```

> ปรับค่าตามจำนวนช่องที่มีใน pattern  
> หาก pattern แต่ละแถวมี 19 ตัว (O/X/F) ต้องตั้งค่าเป็น 19

---

## 🧠 3. โครงสร้างของ Pattern

ใน `config.h` จะมีการกำหนด pattern ของแต่ละ node แยกตามประเภทการทำงาน เช่น LED, M1, M2  

ตัวอย่างโครงสร้าง:

```cpp
// Pattern 1: Motion sequence 1
static const char *P1_LED[6] = {
  "O O O F F X F F X F F O F F O O O O F",
  ...
};

static const char *P1_M1[6] = {
  "O O O F F F F F F F F O F F O O O O F",
  ...
};

static const char *P1_M2[6] = {
  "O X O F F X F X F F X O F F O O X O F",
  ...
};
```

- **แต่ละบรรทัด = Node หนึ่งตัว**
- **แต่ละ pattern ต้องมี 19 ตัวอักษร (O/X/F)**
  - `O` = เปิด  
  - `X` = สลับ (toggle) *(มอเตอร์ M1 ห้ามใช้)*  
  - `F` = ปิด  

> ⚠️ หากจำนวนตัวอักษรไม่ตรงกับ `PATTERN_NUM_SEQUENCES` โปรแกรมจะทำงานไม่ถูกต้อง

---

## 🌞 4. การทำงานร่วมกับเซ็นเซอร์แสง (VEML7700)

ระบบจะอ่านค่าความสว่างทุก 5 วินาที  
และสลับ pattern โดยอัตโนมัติตามช่วงแสงที่กำหนดไว้ใน `config.h` เช่น

| ช่วงแสง (lux) | Pattern ที่เลือก |
|----------------|------------------|
| < 50           | Pattern 1        |
| 50–300         | Pattern 1        |
| > 300          | Pattern 2        |

> สามารถปรับช่วงได้ตามสภาพแวดล้อมจริง

---

## 🧩 5. ตัวอย่างการแก้ไข Pattern ใหม่

หากต้องการเพิ่ม pattern ใหม่ (เช่น Pattern 3):

1. คัดลอกชุด pattern เดิม `P2_LED`, `P2_M1`, `P2_M2`
2. แก้ชื่อเป็น `P3_LED`, `P3_M1`, `P3_M2`
3. ปรับค่าภายในแต่ละบรรทัด (O/X/F)
4. เพิ่มฟังก์ชันใน `main_base.cpp`:
   ```cpp
   static cro::MotionFrame buildPattern3(uint8_t seq);
   ```
5. เพิ่มเงื่อนไขเลือกในส่วนส่งข้อมูล:
   ```cpp
   if (sequence_selection == 3) { buildPattern3(seq); }
   ```

---

## 🧾 6. สรุปสิ่งที่ผู้ใช้แก้ไขได้

| หมวด | ตัวแปร/ค่า | รายละเอียด |
|------|--------------|-------------|
| 🔹 Pattern | `P1_LED`, `P1_M1`, `P1_M2`, ... | ปรับรูปแบบ ON/OFF |
| 🔹 Motor | `M1_PRESET`, `M2_PRESET` | ปรับความเร็ว, มุม, จำนวนรอบ |
| 🔹 Timing | `PATTERN_TIMEFRAME_MS` | เวลาต่อ step |
| 🔹 Light Sensor | `LIGHT_SENSOR_THRESHOLD_LUX_*` | ปรับช่วงความสว่าง |
| 🔹 Pattern Selection | `sequence_selection` | เริ่มต้นด้วย Pattern ที่ต้องการ |

---

## 🛠️ 7. หมายเหตุสำหรับนักพัฒนา

- หากมีการ include `config.h` ในหลายไฟล์ ให้ประกาศ `extern uint8_t sequence_selection;` ใน `config.h`  
  และ define จริงใน `main_base.cpp` เพื่อลดปัญหา multiple definition
- หากเพิ่มจำนวน sequence ต้องปรับทั้ง pattern และ `PATTERN_NUM_SEQUENCES` ให้ตรงกันเสมอ

---

**จัดทำโดย:**  
YUTTAPICHAI LEELAI
